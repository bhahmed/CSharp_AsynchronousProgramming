<Window x:Class="AsyncAwaitSample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AsyncAwaitSample"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="825">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="1" Margin="10 0" FontSize="28">
            Simple Async Demo App
        </TextBlock>
        <Button x:Name="executeSync" Grid.Row="2" Margin="10" Padding="10" Click="ExecuteSync_OnClick">Normal Execute</Button>
        <Button x:Name="executeAsync" Grid.Row="3" Margin="10" Padding="10" Click="ExecuteAsync_OnClick">Async Execute</Button>
        <TextBlock x:Name="resultsWindow" Grid.Row="4" Margin="10"/>
    </Grid>
</Window>
